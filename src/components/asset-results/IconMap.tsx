
import React from 'react';
import { iconMapForLegacyUsage } from '@/icons/registry';

// Centralized icon map via registry
const iconMap: Record<string, React.ReactNode> = iconMapForLegacyUsage();

export default iconMap;
